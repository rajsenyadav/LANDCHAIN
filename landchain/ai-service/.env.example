# ai-service/.env.example
# Copy to .env and fill values

# Shared secret — must match backend/.env AI_SHARED_SECRET
AI_SHARED_SECRET=landchain-ai-secret-change-this

# Backend URL (where to send AI callback)
BACKEND_URL=http://localhost:5000

# Port
PORT=8000
